[project]
name = "kirocrew-agentcore-control"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "cryptography>=47,<48",
]

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kirocrew_agentcore_control"]
