[project]
name = "kirocrew-agentcore-persistence"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "cryptography==50.0.0",
  "inotify-simple==2.0.1",
]

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kirocrew_agentcore_persistence"]
