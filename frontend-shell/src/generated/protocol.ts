// Generated by tools/generate_protocol_models.py; do not edit.
export const PROTOCOL_VERSION = "kirocrew-agentcore.v1" as const;
export const CLIENT_OPERATIONS = ["connection.hello", "chat.submit", "chat.cancel", "kiro.login.start", "kiro.status", "kiro.logout", "kirocrew.ws.send", "kirocrew.ws.close", "sandbox.prepare_stop", "ping"] as const;
export const SERVER_OPERATIONS = ["connection.ready", "sandbox.state", "kiro.auth_status", "kiro.auth_required", "kiro.authenticated", "request.accepted", "output.delta", "tool.activity", "artifact.created", "request.completed", "checkpoint.committed", "error", "pong"] as const;
export const ERROR_CODES = ["INVALID_MESSAGE", "UNSUPPORTED_PROTOCOL", "FRAME_TOO_LARGE", "SEQUENCE_ERROR", "IDEMPOTENCY_CONFLICT", "AUTHENTICATION_FAILED", "AUTHORIZATION_FAILED", "BINDING_MISMATCH", "SANDBOX_CONFLICT", "CHECKPOINT_FAILED", "PERSISTENCE_RESTORE_FAILED", "KIRO_AUTH_REQUIRED", "KIROCREW_UNAVAILABLE", "FEATURE_UNAVAILABLE_IN_AGENTCORE", "THROTTLED", "INTERNAL_ERROR"] as const;
export const ERROR_CATEGORIES = ["AUTHENTICATION", "AUTHORIZATION", "BINDING", "LIFECYCLE", "PROVISIONING", "TRANSPORT", "KIRO_AUTH", "KIROCREW", "PERSISTENCE", "THROTTLING", "INTERNAL"] as const;

export type ClientOperation = (typeof CLIENT_OPERATIONS)[number];
export type ServerOperation = (typeof SERVER_OPERATIONS)[number];
export type Operation = ClientOperation | ServerOperation;
export type ErrorCode = (typeof ERROR_CODES)[number];
export type ErrorCategory = (typeof ERROR_CATEGORIES)[number];

export interface ProtocolEnvelope {
  readonly version: typeof PROTOCOL_VERSION;
  readonly messageId: string;
  readonly requestId?: string | null;
  readonly operation: Operation;
  readonly sequence: number;
  readonly timestamp: string;
  readonly correlationId: string;
  readonly payload: Readonly<Record<string, unknown>>;
}

export interface ProtocolError {
  readonly code: ErrorCode;
  readonly category: ErrorCategory;
  readonly message: string;
  readonly retryable: boolean;
  readonly correlationId: string;
  readonly details?: Readonly<Record<string, string | number | boolean | null>>;
}
