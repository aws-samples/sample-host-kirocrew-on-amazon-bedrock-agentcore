# Protocol contracts

This directory is the project-owned source of truth for browser, headless-client, control-plane, and runtime protocol contracts. Generated Python and TypeScript models are derived from the versioned schemas kept here; generated files are never edited manually.
